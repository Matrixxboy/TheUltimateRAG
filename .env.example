#############################################
# 🔐 LLM / OpenAI Configuration
#############################################

# OpenAI API key (replace with your real key)
OPENAI_API_KEY="sk-proj-xxxxxxxxxxxxxxxxxxxxxxxx"
MODEL_NAME="gpt-4o-mini-2024-07-18" # Default LLM model to use for generation


#############################################
# 🧩 Application Configuration
#############################################
APP_NAME="TheUltimateRAG" # Application name (used in logs, UI, metadata)
APP_ENV="development" # Application environment: development | staging | production
DEBUG=True # Enable or disable debug mode


#############################################
# 🧠 Vector Store / RAG Configuration
#############################################
VECTOR_DB_PATH="chroma_db_data" # Local path to store vector database files (used by Chroma or similar)
COLLECTION_NAME="rag_collection" # Name of the vector collection used for RAG retrieval

# Dimension of embeddings generated by the model
# Example: OpenAI text-embedding-3-large → 1536
EMBEDDING_DIMENSION=1536


#############################################
# 🐘 PostgreSQL Database Configuration
#############################################
POSTGRES_HOST="localhost" # PostgreSQL server host
POSTGRES_DB="postgres" # PostgreSQL database name
POSTGRES_USER="postgres" # PostgreSQL username
POSTGRES_PASSWORD="postgres" # PostgreSQL password
POSTGRES_PORT="5432" # PostgreSQL port

#############################################
# 🧵 Memory & Context Management
#############################################
MEMORY_WINDOW_SIZE=10 # Number of recent interactions stored in memory window
SHORT_TERM_MEMORY_LIMIT=5 # Maximum short-term memory chunks to retain
MEMORY_WINDOW_LIMIT=5 # Hard limit on memory window size

#############################################
# 🖥️ Runtime / Environment Metadata
#############################################
TERM="unknown" # Terminal type (auto-detected in most environments)
SHELL="unknown" # Shell type (auto-detected in most environments)

#############################################
# 🐍 Redis (Short-Term Memory)
#############################################
REDIS_HOST="localhost"
REDIS_PORT="6379"
REDIS_PASSWORD=""
REDIS_USER="default"
REDIS_DB="0"